// import { IDevice } from "@/interfaces/IDevices";

// type Handler = {
//   allDivices: IDevice[];
//   id: string;
// };

// const addToCartHandler = (allDivice: IDevice[], id: string,cart: IDevice[]) => {
//   const currentDevice = allDivice.find((el) => el.id === id);
//   return currentDevice;
// };
